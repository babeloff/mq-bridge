services:
  redis:
    image: redis:8.8-alpine
    container_name: redis-itest-mq-bridge
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 20s
      retries: 5
