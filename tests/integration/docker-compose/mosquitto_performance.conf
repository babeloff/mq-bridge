listener 1883
allow_anonymous true

# Keep the performance broker in-memory so the benchmark measures MQTT transport
# behavior rather than forcing an fsync on every QoS state change.
persistence false

max_queued_messages 50000

# Match the client-side inflight settings used by the performance paths instead
# of letting the broker clamp the Receive Maximum to its small default.
max_inflight_messages 0
